using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This is a shop where a player can spend shells
/// </summary>
/// <remarks>
/// Additional information about the class can be provided in remarks.
/// </remarks>
public class Shop : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
